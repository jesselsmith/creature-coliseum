const encountersReducer = (state = { encounters: [], monsters: [], players: [] }, action) => {
  switch (action.type) {
    case 'ADD_ENCOUNTER':
      return state
    default:
      return state
  }
}
